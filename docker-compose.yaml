services:
  openclaw:
    image: ghcr.io/openclaw/openclaw:latest
    ports:
      - "18789:18789"
    volumes:
      - openclaw_data:/root/.openclaw
    restart: unless-stopped

volumes:
  openclaw_data:
